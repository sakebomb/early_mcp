[project]
name = "early"
version = "0.1.0"
description = "EARLY time tracking MCP server"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.25.0",
]
